# 同学说明文档：数据融合 + 泛化能力问题

---

## 目录

1. [先搞清楚：你的训练数据从哪来？](#1-先搞清楚你的训练数据从哪来)
2. [正确流程：怎么融笑话数据进去？](#2-正确流程怎么融笑话数据进去)
3. [常见踩坑与解决](#3-常见踩坑与解决)
4. [泛化问题：为什么模型只会背模板？](#4-泛化问题为什么模型只会背模板)
5. [修复方案：怎么改代码提升泛化？](#5-修复方案怎么改代码提升泛化)

---

## 1. 先搞清楚：你的训练数据从哪来？

项目有**两条数据路径**，产出完全不同：

```
路径A — 全流程 (scripts/step1~8)
  step1(加载) → step2(清洗) → step3(笑话分类) → step4(融合) → step5(分词)
  → 输出: data/split/train_chatml.json
  → 内容: 2000 条带笑话/故事推荐的四轮对话
  → 效果: 模型会推荐故事/笑话，但可能泛化差

路径B — 单脚本 (multi_turn_prepare_data.py)
  直接读取 jiandanxinli → ChatML 格式
  → 输出: data/multi_turn_qa.json
  → 内容: 59,501 条纯心理咨询对话（无笑话/故事）
  → 效果: 模型根本不会推荐故事/笑话
```

**关键结论**：如果你用的是路径B的数据训练，模型回答里绝对不会有"要不要听个笑话/故事？"——因为训练数据里根本没有这句话。

---

## 2. 正确流程：怎么融笑话数据进去？

### 2.1 最简单的方式：一键全流程

```bash
cd 你的项目目录
python scripts/run_all.py
```

这会按顺序执行 step1 → step8，在 step4 完成笑话与心理咨询数据的融合。

### 2.2 如果一键跑不通，分步跑

```bash
# ① 加载数据（验证两份数据集能正常读取）
python scripts/step1_load_data.py

# ② 清洗（去广告、去无效社交、去重）
python scripts/step2_clean_data.py

# ③ 笑话/故事分类（给笑话库打标签）⭐
python scripts/step3_classify.py

# ④ 跨文件融合（心理咨询 + 笑话拼成四轮对话）⭐⭐
python scripts/step4_fusion.py

# ⑤ 分词 + 划分训练集/测试集
python scripts/step5_tokenize_split.py

# ⑥ 之后的 step6~8 是模型搭建和训练
```

### 2.3 数据流全景图

```
jiandanxinli (3万条)  +  psy525 (26万条)  │  joke_story (16.7万条)
         │                                 │         │
         └── step2 清洗 ───────────────────┘         │
                     │                               │
                     ▼                               ▼
               cleaned_data.json           step3 分类 (规则+API)
               (18.6万条文本)                    │
                     │                           ▼
                     │               joke_story_classified.json
                     │               (故事5080 / 笑话12420 / 诗歌2780)
                     │                           │
                     └────── step4 融合 ◄────────┘
                                 │
                                 ▼
                        fused_dialogue.json
                        (2000 条四轮对话)
                        每条结构:
                          Q1 用户情感倾诉
                          A1 共情回复 + 推荐故事/笑话
                          Q2 用户索要
                          A2 故事/笑话内容
                                 │
                          step5 分词 + 划分
                                 │
                                 ▼
                    train_chatml.json (1600条)
                    test_chatml.json  (400条)
```

---

## 3. 常见踩坑与解决

### 坑1：step3_classify.py 卡住或报错

```
❌ 报错: ModuleNotFoundError: No module named 'openai'
```

**原因**：step3 需要调用豆包 API 做精分验证，需要 `openai` 库。

**解决**：
```bash
pip install openai
```

如果装了还不行（API key 过期或网络不通），**代码有纯规则兜底模式**——不装 openai 也能跑，只是分类精度稍低：

```
⚠️  未安装 openai 库，跳过 API 精分（仅使用规则分类）
```

**结论：不装 openai 也能完成 step3，不影响后续融合。**

### 坑2：提示 "未找到融合数据"

```
❌ 未找到融合数据，请先运行 Step 4
```

**原因**：step4 没跑成功，融合对话没有生成。

**排查**：
```bash
ls data/fused/fused_dialogue.json   # 这个文件存在吗？
ls data/classified/joke_story_classified.json  # 分类数据存在吗？
ls data/cleaned/cleaned_data.json   # 清洗数据存在吗？
```

缺少哪个就重新跑对应的 step。

### 坑3：分步跑时 cwd 不对

step 脚本里用的是相对路径，**必须在项目根目录运行**：

```bash
cd E:\work\Claude code default\自然语言处理期末   # 先 cd 到根目录
python scripts/step4_fusion.py                    # 再跑脚本
```

### 坑4：Windows 中文乱码

```bash
set PYTHONIOENCODING=utf-8
python scripts/step4_fusion.py
```

### 坑5：step4 融合出的对话全是"日常"情感

step4 用关键词匹配检测情感，如果文本不包含任何关键词，就会被归为"日常"。这是正常的，只要融合对话数量达到 2000 条就行。

### 坑6：训练数据太少，GPU 也慢？

step7 有 `TRAIN_SUBSET` 参数可以调：

```python
# step7_train_test.py 第29行
TRAIN_SUBSET = 150  # GTX 1060: 150条, RTX 3090: 可以设 0(全量) 或更大
```

---

## 4. 泛化问题：为什么模型只会背模板？

### 4.1 症状

- 不管用户说什么，模型第一句回复必然以"要不要听个故事/笑话？"结尾
- 用户说"我不想听"，模型不知道怎么接
- 用户连续倾诉情感（不索要故事），模型还是在第二轮强行推荐
- 回答内容像在"背课文"，换个问法就不会了

### 4.2 根因

打开 `scripts/step4_fusion.py`，看第 230 行：

```python
dialogue = {
    "emotion": emotion,
    "category": category,
    "conversations": [
        {"role": "user", "content": question_text},         # ← 情感倾诉
        {"role": "assistant", "content": f"{empathic_reply} 要听个{category}吗？{ask_reason}"},  # ← 必推荐
        {"role": "user", "content": user_ask},              # ← 必索要
        {"role": "assistant", "content": story_text},       # ← 讲故事
    ]
}
```

**全部 2000 条训练数据都是这个完全相同的四轮模板。**

| 问题 | 具体表现 | 代码位置 |
|------|---------|---------|
| 第2轮 assistant **必定推荐** | 42个模板全以"要不要听个故事/笑话"结尾 | L95-161 |
| 第3轮 user **必定索要** | 10种索要语，零拒绝 | L164-175 |
| 第4轮 assistant **复制粘贴** | 直接从笑话库取原文 | L215 |
| **零结构变化** | 全是 4 轮，没有 1/2/3/5+ 轮对话 | L230-240 |

### 4.3 模型到底学到了什么？

```
模型视角（训练时只见过这些）:
  "哦，对话就是这样：
   用户说难过的事 → 我推荐故事 → 用户说好 → 我讲
   没有其他可能性。"

推理时:
  用户: "我今天心情不好..."
  模型: → 必须走模板 → "要不要听个故事？"  ← 唯一会做的事

  用户: "不用了，我就想聊聊"  
  模型: → 懵了，训练数据里没有这个分支 →
         可能还是强行推荐，或者生成乱码
```

---

## 5. 修复方案：怎么改代码提升泛化？

### 方案1：打破固定结构（最有效，改动 step4）

在融合阶段混入**不同结构**的对话，而不是 100% 四轮推荐模板。

**修改 `scripts/step4_fusion.py` 的 `generate_fused_dialogue()` 函数：**

```python
def generate_fused_dialogue(question_text, story_item, emotion):
    """构造多样化的对话结构"""
    story_text = story_item.get("text", "") or story_item.get("content", "")
    category = story_item.get("category", "其他")

    # 随机选择对话结构（50%推荐，30%纯共情，20%交替）
    pattern = random.choice(["recommend", "recommend", "pure_empathy", "pure_empathy", "pure_empathy", "alt"])

    if pattern == "recommend":
        # 原有逻辑：四轮推荐
        empathic_reply = generate_empathic_reply(emotion)
        user_ask = random.choice(USER_ASK_PHRASES)
        dialogue = {
            "emotion": emotion, "category": category,
            "conversations": [
                {"role": "user", "content": question_text},
                {"role": "assistant", "content": f"{empathic_reply} 要听个{category}吗？{ask_reason}"},
                {"role": "user", "content": user_ask},
                {"role": "assistant", "content": story_text},
            ]
        }

    elif pattern == "pure_empathy":
        # 纯共情：不推荐故事/笑话，只进行心理咨询
        reply1 = generate_empathic_reply_no_recommend(emotion)  # 新增函数
        user_reply = random.choice(USER_CONTINUE_PHRASES)        # 新增变量
        reply2 = generate_followup_empathy(emotion)               # 新增函数
        dialogue = {
            "emotion": emotion, "category": "无",
            "conversations": [
                {"role": "user", "content": question_text},
                {"role": "assistant", "content": reply1},          # ← 纯共情，不推荐
                {"role": "user", "content": user_reply},           # ← 用户继续倾诉
                {"role": "assistant", "content": reply2},          # ← 继续共情
            ]
        }

    elif pattern == "alt":
        # 交替模式：用户拒绝推荐，模型尊重意愿
        empathic_reply = generate_empathic_reply(emotion)
        user_decline = random.choice(USER_DECLINE_PHRASES)         # 新增：拒绝语
        fallback_reply = generate_fallback_empathy(emotion)        # 新增：尊重意愿
        dialogue = {
            "emotion": emotion, "category": category,
            "conversations": [
                {"role": "user", "content": question_text},
                {"role": "assistant", "content": f"{empathic_reply} 要听个{category}吗？"},
                {"role": "user", "content": user_decline},         # ← 用户拒绝
                {"role": "assistant", "content": fallback_reply},  # ← 尊重选择，继续共情
            ]
        }

    return dialogue
```

### 方案2：新增模板函数

在 step4_fusion.py 中追加以下内容：

```python
# === 新增：纯共情模板（不推荐故事/笑话） ===
EMPATHY_NO_RECOMMEND = {
    "焦虑": [
        "我能感受到你的焦虑，这种感觉真的很不好受。你愿意多说说是哪些事情让你这么紧张吗？",
        "焦虑的时候有人倾听就很重要。我在，你慢慢说。",
        "试试深呼吸，把注意力放在当下。你能描述一下现在最让你担心的是什么吗？",
        "压力确实会让人透不过气。你平时有什么方式可以让自己稍微放松一点吗？",
    ],
    "抑郁": [
        "我听到你的难过了，这种感受很真实。你愿意跟我多聊聊吗？",
        "你愿意把这些说出来已经很勇敢了。这种感觉持续多久了？",
        "低落的时候，也许不需要急着走出来，我们可以先聊聊。",
        "我在这里陪着你。你想说说最近发生了什么事吗？",
    ],
    # ... 其他情感同理
}

# === 新增：用户继续倾诉语（而非索要故事） ===
USER_CONTINUE_PHRASES = [
    "我也不知道该怎么办，就是很难受",
    "我试过很多方法都没用",
    "有时候我觉得是不是我自己的问题",
    "周围没人理解我，我只能自己扛着",
    "你能理解这种感觉吗？",
    "谢谢你听我说这些",
    "这种情况已经持续很久了",
    "我真的很迷茫",
]

# === 新增：用户拒绝推荐语 ===
USER_DECLINE_PHRASES = [
    "不用了，我就想聊聊",
    "不想听故事，你能理解我的感受吗？",
    "算了，听了也开心不起来",
    "其实我更想知道怎么解决这个问题",
    "不用讲笑话了，你能给我一些建议吗？",
    "我现在笑不出来...",
]

# === 新增：尊重用户拒绝后的回复 ===
FALLBACK_EMPATHY = {
    "焦虑": [
        "没关系，不想听故事也没事。说说看，现在最让你焦虑的是什么呢？",
        "好的，不勉强你。我们聊聊你最近的情况吧。",
    ],
    "抑郁": [
        "理解，我现在就陪着你。你想聊什么都可以。",
        "没关系的，我们就这样聊聊也挺好。你最近睡眠怎么样？",
    ],
    # ... 其他情感同理
}


def generate_empathic_reply_no_recommend(emotion):
    """纯共情回复，不推荐故事/笑话"""
    templates = EMPATHY_NO_RECOMMEND.get(emotion, EMPATHY_NO_RECOMMEND.get("日常", ["说说看，最近怎么样？"]))
    return random.choice(templates)


def generate_followup_empathy(emotion):
    """深度共情追问"""
    followups = {
        "焦虑": ["这种感觉一定很辛苦。你觉得焦虑是从什么时候开始的？", "我理解。除了担心，你身体上有什么感觉吗？"],
        "抑郁": ["谢谢你的信任。这种感受持续多久了？", "你辛苦了。除了跟你说的这些，还有什么想聊聊的吗？"],
        # ... 其他情感
    }
    templates = followups.get(emotion, ["谢谢你告诉我这些。还有什么想说的吗？"])
    return random.choice(templates)


def generate_fallback_empathy(emotion):
    """被拒绝后的尊重回复"""
    templates = FALLBACK_EMPATHY.get(emotion, ["没关系，你想聊什么都可以。"])
    return random.choice(templates)
```

### 方案3：混入真实对话（利用已有数据）

`multi_turn_prepare_data.py` 已能从原始数据提取 15,845 条真实多轮对话。把这些**不加四轮模板修饰**的对话混入训练集：

```python
# 在 step5_tokenize_split.py 的 main() 中，加载融合数据后加入：
def load_real_dialogues():
    """从 multi_turn_prepare_data.py 的输出中取真实对话"""
    path = os.path.join(BASE_DIR, "data", "multi_turn_qa.json")
    if not os.path.exists(path):
        print("  ⚠️ 未找到真实对话数据，先运行 multi_turn_prepare_data.py")
        return []
    with open(path, "r", encoding="utf-8") as f:
        data = [json.loads(line) for line in f if line.strip()]
    # 随机取 500 条混入训练集，稀释模板对话占比
    random.shuffle(data)
    return data[:500]

# 在 main() 中加入:
real_dialogues = load_real_dialogues()
all_dialogues = fused_dialogues + real_dialogues  # 混合！
random.shuffle(all_dialogues)
```

### 方案4：降低重复惩罚参数

训练超参数里 `REPETITION_PENALTY` 设 1.1 偏保守，可以试试 1.15-1.2 让模型更不容易重复背诵。

---

## 快速检查清单

在改代码之前，先确认你确实遇到了这些问题：

| 检查项 | 怎么测 |
|--------|--------|
| 训练数据有没有笑话？ | 打开 `data/split/train_chatml.json`，搜索"故事"或"笑话"看看有没有 |
| step3 跑通了吗？ | 检查 `data/classified/joke_story_classified.json` 是否存在 |
| step4 跑通了吗？ | 检查 `data/fused/fused_dialogue.json` 是否存在，不少于 1000 条 |
| 模型是否只会说"要听个...吗"？ | 随便发一句情感倾诉，看回复是不是必然以推荐结尾 |
| 用户说"不想听"，模型怎么回？ | 如果在 Web 界面测试，试试拒绝推荐看模型反应 |

---

## 总结

```
你要做的事：

1. 确认用的是全流程（step1~5），不是单脚本
   → 如果 data/split/train_chatml.json 里没有"故事""笑话"关键词
   → 说明没融合，重跑 step1~5

2. step3 卡住不要慌
   → 没装 openai 也能跑，纯规则模式够用

3. 解决泛化问题
   → 改 step4_fusion.py，加入纯共情和拒绝分支
   → 混入 multi_turn_prepare_data.py 的真实对话
   → 重新跑 step4 → step5 → step7
```

---

*写于 2026/06/24，有问题在群里问*
