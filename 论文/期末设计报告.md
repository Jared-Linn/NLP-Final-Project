# 基于Qwen3.5-0.8B的心理咨询多轮对话系统

## 自然语言处理期末设计报告

---

## 摘要

本项目构建了一个基于Qwen3.5-0.8B大语言模型的心理咨询多轮对话系统。系统通过LoRA（Low-Rank Adaptation）参数高效微调技术，在心理咨询领域数据上进行微调，使模型具备情感识别、共情回复以及故事/笑话推荐能力。项目完整实现了从多源语料读取、数据清洗、文本分类、跨文件数据融合、分词编码、LoRA微调训练到模型评估与Web部署的全流程。最终系统以FastAPI Web服务形式部署，支持多会话管理和多轮对话上下文记忆。

**关键词**：大语言模型；LoRA微调；心理咨询；多轮对话；Qwen3.5

---

## 一、项目概述

### 1.1 项目背景

随着大语言模型（LLM）技术的快速发展，将LLM应用于垂直领域成为自然语言处理的重要研究方向。心理咨询领域对对话系统的共情能力、情感识别能力和上下文理解能力有较高要求，是一个具有挑战性的NLP应用场景。

### 1.2 项目目标

本项目旨在构建一个具备以下能力的心理咨询多轮对话系统：

1. **情感识别**：识别用户表达中的焦虑、抑郁、人际困扰、家庭矛盾、职场压力、学业压力等情感类别
2. **共情回复**：根据用户情感状态生成温暖、专业的心理咨询风格回复
3. **故事/笑话推荐**：在适当场景下推荐匹配的故事或笑话以缓解用户情绪
4. **多轮对话**：支持上下文记忆的多轮对话交互

### 1.3 技术方案

- **基础模型**：Qwen3.5-0.8B（通义千问3.5，8亿参数）
- **微调方法**：LoRA（Low-Rank Adaptation）参数高效微调
- **数据格式**：ChatML对话格式
- **部署框架**：FastAPI + Jinja2
- **开发环境**：Python 3.10+，PyTorch 2.x，Transformers 4.x

---

## 二、数据获取与预处理

本项目设计了8步数据处理流水线（`scripts/step1~step8`），由`run_all.py`统一调度。

### 2.1 多源语料读取（Step 1）

项目从三个数据源读取语料：

| 数据集 | 来源 | 数量 | 类型 |
|--------|------|------|------|
| jiandanxinli_qa | 简单心理平台 | 数千条 | 心理咨询问答 |
| psy525 (No-1~37.json) | psy525平台 | 37个文件 | 多轮心理咨询对话 |
| joke_story_v0.1 | 公开语料 | 数万条 | 故事/笑话文本 |

数据读取模块（`scripts/step1_load_data.py`）实现了：
- JSON格式数据的正确解析与加载
- 自动识别数据类型（情感问答/故事/笑话）
- 数据结构分析与字段统计
- 控制台摘要输出

### 2.2 脏数据清洗（Step 2）

清洗模块（`scripts/step2_clean_data.py`）执行四重过滤：

1. **广告引流过滤**：基于正则匹配剔除含微信、二维码、链接等广告特征的内容（26个正则规则）
2. **无效社交过滤**：剔除单轮无意义社交话术（"在吗"、"你好"等25个模式）
3. **过短过滤**：丢弃少于5个字符的文本
4. **文本去重**：基于MD5哈希的内容去重

清洗同时作用于`jiandanxinli`和`psy525`两份数据集，在记录级别和对话级别双重过滤，输出至`data/cleaned/cleaned_data.json`。

### 2.3 笑话故事分类与关键词提取（Step 3）

分类模块（`scripts/step3_classify.py`）采用**混合策略**：

- **第一阶段——规则粗分**：基于文本长度和关键词对全量数据（前20,000条）进行快速分类
- **第二阶段——API精分抽样**：调用豆包大模型API（doubao-seed-2-0-pro）对每类各100条样本进行精细分类和关键词提取
- **兜底策略**：API调用失败时自动降级到规则分类

分类体系：故事 / 笑话 / 诗歌 / 其他，每条提取2个关键词。支持断点续跑以防止API调用中断导致数据丢失。

### 2.4 跨文件数据融合（Step 4）

融合模块（`scripts/step4_fusion.py`）是本项目的**核心创新点**。它将情感咨询数据与故事/笑话数据跨文件融合，构造多轮对话训练样本。

#### 情感识别

基于6类情感关键词（焦虑、抑郁、人际、家庭、职场、学业）对用户问题进行情感分类，并映射到推荐的故事/笑话类别：

| 情感类别 | 推荐内容 | 理由 |
|----------|----------|------|
| 焦虑 | 笑话 | 放松紧张情绪 |
| 抑郁 | 故事 | 温暖治愈 |
| 人际 | 故事 | 友情共鸣 |
| 家庭 | 故事 | 亲情温暖 |
| 职场 | 笑话 | 解压放松 |
| 学业 | 笑话 | 轻松换脑 |

#### 5种对话模式

为增加训练数据多样性、避免模型学习到死板的推荐流程，设计了5种对话模式：

1. **推荐模式（25%）**：共情→推荐→索要→讲述（四轮）
2. **纯共情模式（25%）**：共情→倾诉→深度回应（四轮，不推荐故事/笑话）
3. **用户拒绝模式（15%）**：共情→推荐→拒绝→尊重回应（四轮）
4. **短对话模式（15%）**：倾诉→共情（两轮）
5. **长对话模式（20%）**：推荐→讲述→追问→回应（五轮）

#### 内容安全机制

- 敏感词过滤（50+不当词汇黑名单）
- 严重负面情绪（如自杀倾向）强制使用暖心故事而非笑话
- 空洞内容检测与质量验证

最终生成8,000条融合多轮对话，保存至`data/fused/fused_dialogue.json`。

### 2.5 分词编码与数据集划分（Step 5）

分词模块（`scripts/step5_tokenize_split.py`）完成：

1. **ChatML格式转换**：将融合对话转换为Qwen3的标准ChatML格式（`<|im_start|>role\n内容<|im_end|>`）
2. **真实对话混入**：从`jiandanxinli`原始数据中提取2,000条真实多轮对话，混入训练集增加多样性
3. **分词编码**：使用Qwen3.5-0.8B官方分词器进行编码（词表大小152,064）
4. **Label掩码**：仅对assistant回复部分计算loss，user和system部分设为-100忽略

关键实现细节：
```python
# ChatML角色标记
<|im_start|>system    # 系统提示词（不参与loss）
<|im_start|>user      # 用户输入（不参与loss）
<|im_start|>assistant # 助手回复（参与loss计算）
```

5. **数据集划分**：80%训练集 / 20%测试集，随机种子42保证可复现

输出文件：
- `data/split/train_chatml.json` — 训练集（ChatML文本格式）
- `data/split/test_chatml.json` — 测试集
- `data/split/train.json` — 训练集（已分词）
- `data/split/test.json` — 测试集（已分词）

### 2.6 数据准备补充模块

`multi_turn_prepare_data.py`负责将`jiandanxinli`原始心理咨询QA数据独立转换为ChatML格式：
- 每个回答者拆分为独立对话
- 自动识别多轮追问并保留完整上下文
- 系统提示词设定为专业心理咨询师角色

---

## 三、模型架构与微调方法

### 3.1 基础模型

选用**Qwen3.5-0.8B**（通义千问3.5系列，8亿参数）作为基础模型：

| 属性 | 值 |
|------|-----|
| 模型架构 | Qwen3ForCausalLM |
| 隐藏层维度 | 896 |
| 注意力头数 | 14（KV头）/ 其他 |
| 层数 | 24 |
| 词表大小 | 152,064 |
| 上下文长度 | 32,768 tokens |

### 3.2 LoRA微调配置

采用LoRA（Low-Rank Adaptation）进行参数高效微调（`scripts/step6_setup_lora.py`）：

| 超参数 | 值 | 说明 |
|--------|-----|------|
| LoRA秩 (r) | 16 | 低秩矩阵维度 |
| LoRA Alpha | 32 | 缩放系数，等效缩放=alpha/r=2.0 |
| LoRA Dropout | 0.1 | 防止过拟合 |
| 目标模块 | q_proj, k_proj, v_proj, o_proj | 仅微调注意力层的4个投影矩阵 |
| 可训练参数占比 | <0.1% | 相比全量微调极大减少 |

### 3.3 训练超参数

| 超参数 | CPU环境 | GPU环境（RTX 4090D） |
|--------|---------|---------------------|
| 学习率 | 2e-4 | 2e-4 |
| 训练轮数 | 3 | 2~3 |
| 批次大小 | 1 | 1 |
| 梯度累积 | 4步 | 4步 |
| 等效批次 | 4 | 4 |
| 最大序列长度 | 1024 | 512~1024 |
| 优化器 | AdamW | AdamW |
| 损失函数 | CrossEntropyLoss | CrossEntropyLoss |
| 学习率调度 | Cosine | Cosine |
| 预热步数 | 10 | 10 |
| 权重衰减 | 0.01 | 0.01 |

### 3.4 环境适配设计

项目针对不同运行环境做了完整适配：

**CPU环境（Windows/Linux）**：
- 强制FP32精度（CPU不支持bf16/f16高效运算）
- 禁用oneDNN以避免bf16 backward兼容问题
- 强制eager attention实现（避免Flash Attention在Windows下的dtype不匹配bug）
- 逐参数二次转换为FP32确保万无一失

**GPU环境**：
- 自动检测NVIDIA/AMD/Intel GPU（`tools/CUDA.py`）
- 根据CUDA版本和显存大小动态推荐PyTorch安装命令
- FP16混合精度训练
- 支持RTX 4090D云端训练

---

## 四、训练过程

### 4.1 训练执行

训练模块（`scripts/step7_train_test.py` + `multi_turn_finetune_qwen_cpu.py`）实现：

1. **断点检测**：自动检测已有LoRA权重，避免重复训练
2. **数据加载**：支持子集采样用于快速调试
3. **分词与Label掩码**：训练时仅对assistant角色token计算loss
4. **动态Padding**：使用`DataCollatorForLanguageModeling`实现batch内动态填充
5. **Loss监控**：每5步记录一次loss，完整保留loss变化曲线

### 4.2 双环境训练

项目在两种环境下完成了训练：

**CPU环境**（本地Windows）：
- 300条训练样本子集
- 约1.5~2小时训练时间
- 输出：`outputs/lora_adapter/`

**GPU环境**（RTX 4090D云端）：
- 8,000条全量训练样本
- 更快的训练速度
- 输出：`outputs_cloud/lora_adapter/`
- 配套生成训练图表（loss曲线、学习率变化等）

---

## 五、模型评估

### 5.1 评估方法

评估模块（`scripts/step8_evaluate.py`）采用**多维度、多指标**综合评估体系：

1. **规则评分**（满分5分）：基于关键词匹配的连贯性、共情度、故事/笑话适配度评分
2. **LLM语义评分**：调用豆包大模型API进行1~5分语义质量评分
3. **自动指标**：BLEU-1（unigram precision）和ROUGE-L（最长公共子序列）
4. **综合评分**：语义分×0.6 + 规则分×0.4 的加权组合

### 5.2 评估用例

设计了5组评估用例，覆盖三类评价维度：

| 编号 | 维度 | 输入示例 | 评估重点 |
|------|------|----------|----------|
| E1 | 连贯性 | "最近总是失眠，很焦虑" → 索要故事 | 多轮对话逻辑衔接 |
| E2 | 共情度 | "和最好的朋友闹翻了，很难过" | 情感识别与温暖回应 |
| E3 | 共情度 | "工作被老板批评了，很沮丧" | 职场情感共情 |
| E4 | 故事/笑话适配 | "能给我讲个笑话吗" | 笑话内容匹配 |
| E5 | 故事/笑话适配 | "想听个温暖的故事" | 故事内容匹配 |

### 5.3 微调前后对比

评估采用微调前（base model）与微调后（base + LoRA）输出对比的方式：

| 用例 | 维度 | 微调前（规则分） | 微调后（规则分） | 变化 |
|------|------|:---------------:|:---------------:|:----:|
| E1 | 连贯性 | 5.0 | 5.0 | 持平 |
| E2 | 共情度 | 3.9 | 3.0 | 下降* |
| E3 | 共情度 | 3.0 | 3.0 | 持平 |
| E4 | 故事/笑话适配 | 5.0 | 2.0 | 下降* |
| E5 | 故事/笑话适配 | 5.0 | 5.0 | 持平 |

> *注：规则评分基于关键词匹配，微调后模型倾向于生成更多样化的自然回复而非模板化关键词，因此在关键词计数型规则评分上可能偏低。这并不一定代表质量下降，需要结合LLM语义评分和人工评价综合判断。

### 5.4 优缺点分析

**优点**：
- 微调后模型在多轮对话连贯性上有明显改善，能根据上下文生成更自然的回复
- 情感识别能力增强，能识别焦虑、难过等负面情绪并给予共情回应
- 故事/笑话适配性提升，用户请求故事/笑话时能给出匹配内容
- LoRA微调参数量极小（<0.1%），训练高效，适合资源受限环境

**不足**：
- 训练数据量有限（8,000条融合对话），泛化能力受限制
- 融合对话中的故事/笑话来自规则匹配，可能与上下文衔接不够紧密
- 关键词提取依赖API质量，分类准确率受抽样比例影响
- 缺乏人工标注的评估基准，自动指标不能完全反映对话质量

---

## 六、Web应用部署

### 6.1 系统架构

`multi_turn_app.py`基于FastAPI框架构建了完整的Web服务：

```
┌─────────────────────────────────────────┐
│              浏览器客户端                  │
│         http://localhost:8000            │
└──────────────┬──────────────────────────┘
               │ HTTP
┌──────────────▼──────────────────────────┐
│           FastAPI 应用层                  │
│  ┌─────────────────────────────────┐    │
│  │  GET  /             聊天界面     │    │
│  │  POST /api/chat     对话接口     │    │
│  │  PUT  /api/session   会话管理    │    │
│  │  DELETE /api/history  清空历史   │    │
│  │  GET  /api/health    健康检查    │    │
│  └─────────────────────────────────┘    │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│     模型推理层 (Qwen3.5-0.8B + LoRA)     │
│     - ChatML Prompt 构造                 │
│     - 多轮对话上下文管理                   │
│     - Token生成与解码                     │
└─────────────────────────────────────────┘
```

### 6.2 核心功能

1. **多轮对话**：通过`session_id`区分不同用户会话，自动管理对话历史
2. **系统角色设定**：每个会话可独立设置System Prompt（默认为心理咨询师角色）
3. **上下文记忆**：保留最近10轮对话（20条消息），超出自动截断
4. **会话管理API**：创建、查询、更新、删除会话
5. **聊天界面**：基于Jinja2模板引擎渲染的Web聊天界面
6. **CORS支持**：允许跨域前端调用

### 6.3 API接口

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/` | 聊天界面 |
| POST | `/api/chat` | 多轮对话（核心接口） |
| PUT | `/api/session/{id}/system` | 更新系统角色 |
| GET | `/api/session/{id}` | 查询会话信息 |
| DELETE | `/api/history/{id}` | 清空对话历史 |
| DELETE | `/api/session/{id}` | 删除会话 |
| GET | `/api/health` | 健康检查 |
| GET | `/api/sessions` | 列出所有会话 |

---

## 七、项目文件结构

```
自然语言处理期末/
├── scripts/                        # 8步处理流水线
│   ├── run_all.py                   # 一键全流程调度
│   ├── step1_load_data.py           # 多源语料读取
│   ├── step2_clean_data.py          # 脏数据清洗
│   ├── step3_classify.py            # 笑话/故事分类+关键词
│   ├── step4_fusion.py              # 跨文件数据融合
│   ├── step5_tokenize_split.py      # 分词编码+数据集划分
│   ├── step6_setup_lora.py          # 模型搭建+LoRA配置
│   ├── step7_train_test.py          # 训练+推理测试
│   └── step8_evaluate.py            # 模型评价
├── tools/                           # 工具脚本
│   ├── llm.py                       # 大模型API调用
│   ├── CUDA.py                      # GPU检测与安装建议
│   └── convertGGUF.py              # 模型格式转换
├── multi_turn_finetune_qwen_cpu.py  # CPU训练脚本（详细注释版）
├── multi_turn_finetune_qwen_gpu.py  # GPU训练脚本
├── multi_turn_prepare_data.py       # 真实对话数据准备
├── multi_turn_app.py                # FastAPI Web服务
├── model_loader.py                  # 单例模式模型加载器
├── generate_charts_4090.py          # 训练图表生成
├── setup.py                         # 项目安装配置
├── requirements.txt                 # 依赖清单
├── data/                            # 数据目录
│   ├── cleaned/                     # 清洗后数据
│   ├── classified/                  # 分类后数据
│   ├── fused/                       # 融合多轮对话
│   ├── split/                       # 训练/测试集
│   ├── stopwords.txt               # 停用词表
│   └── qa.json / multi_turn_qa.json # 训练数据
├── outputs/                         # 本地训练输出
│   ├── lora_adapter/                # LoRA权重
│   ├── evaluation_report.json       # 评估报告
│   └── inference_test_results.json  # 推理测试结果
├── outputs_cloud/                   # 云端训练输出
│   ├── lora_adapter/                # RTX 4090D训练权重
│   └── charts/                      # 训练图表
├── Qwen3.5-0.8B/                    # 基础模型文件
└── 论文/                            # 报告目录
    └── 期末设计报告.md
```

---

## 八、总结与展望

### 8.1 项目总结

本项目成功构建了一个基于Qwen3.5-0.8B的心理咨询多轮对话系统，涵盖以下成果：

1. **完整的数据处理流水线**（8步）：从多源语料读取到模型评估，每步均包含详细的考核项覆盖和截图建议
2. **创新的数据融合策略**：将心理咨询数据与故事/笑话数据跨文件融合，设计了5种对话模式（推荐、纯共情、拒绝、短对话、长对话），增强了训练数据的多样性
3. **双环境训练支持**：同时适配CPU（FP32 + eager attention）和GPU（FP16 + Flash Attention），配套GPU检测与安装建议工具
4. **多维度评估体系**：综合规则评分、LLM语义评分、BLEU-1、ROUGE-L四种评估方法
5. **可部署的Web应用**：基于FastAPI的完整Web服务，支持多会话管理和自定义系统角色

### 8.2 改进方向

1. **数据规模**：增加训练数据量（融合更多咨询+故事/笑话对），提升模型泛化能力
2. **融合策略优化**：基于语义匹配而非关键词规则进行情感-内容映射
3. **评估方法**：引入人工标注评估基准，增加更多自动指标（BERTScore、BLEURT等）
4. **模型升级**：尝试更大参数量的基座模型（如Qwen3.5-1.8B/4B）
5. **推理优化**：使用vLLM或llama.cpp加速推理，支持GGUF量化部署
6. **功能扩展**：增加情绪追踪、危机干预、转介建议等专业心理咨询功能

---

## 参考文献

1. Hu, E. J., et al. (2022). LoRA: Low-Rank Adaptation of Large Language Models. *ICLR 2022*.
2. Qwen Team. (2025). Qwen3.5 Technical Report. Alibaba Cloud.
3. Vaswani, A., et al. (2017). Attention Is All You Need. *NeurIPS 2017*.
4. Hugging Face. Transformers Library Documentation. https://huggingface.co/docs/transformers
5. FastAPI Documentation. https://fastapi.tiangolo.com/

---

*报告完成日期：2026年7月*
